# Android / Kotlin
*.iml
.gradle
build/
captures/
.externalNativeBuild
.cxx
*.apk
*.aab
*.ap_
*.dex

# Local SDK path (machine-specific)
local.properties

# IDE - IntelliJ/Android Studio
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Kotlin
*.class
*.kotlin_module

# Config files with secrets (API keys, tokens, URLs)
api.properties
*.env
secrets.properties

# Firebase project config (per-developer/CI, not shared in repo)
app/google-services.json

# Local planning docs (specs/plans) - not shared to the remote repo
docs/superpowers/
